# 服装关键点：3 张测试图的问题诊断记录

最近一次验证（2026-06-12 提交 `9ac6fe9` 之后）跑 3 张衣服图（蓝旗袍 /
浅粉 T 恤 / 白衬衫），记录每张图的关键点状态和根因，作为以后调几何
派生时的参考。

## 蓝旗袍（image.png）

**状态**：✅ 8 关键点全部对位

**主分割还是 rembg fallback**：主分割（色彩距离）通过，foreground 39%。

**无问题**。

## 浅粉 T 恤（image2.jpg）

**状态**：✅ 8 关键点全部对位

**主分割还是 rembg fallback**：rembg fallback。浅粉 vs 浅蓝背景，色彩距离
判出几乎全图为前景（97%），触发 `_mask_is_usable` 的上界阈值 (70%)，
转入 rembg。

**rembg fallback 路径下关键点正确**——证明 fallback 本身有效。

## 白衬衫+衣架（image3.png）

**状态**：❌ top_center 错位（落在右侧口袋附近），左右肩/腋点连带错

**一开始的误判**：以为是衣架污染 rembg mask。
任务 #50 因此记录为"rembg 把衣架和衬衫合并为同一连通域，几何派生
无法识别领口"。

**实际看 mask 后纠正**（任务 #57 完成诊断）：
rembg 输出显示衣架和衬衫**已经是分离的两个连通域**——衣架钩是
顶部的小三角，衬衫是中间大片完整轮廓（左右肩对称、左右袖可见、
左右下摆对称），两者之间有 1~2 像素的间隙。**rembg 工作正确**，
不需要为衣架做 mask 形态学清理。

**真正根因**：白衬衫是**翻领**（V 形领口），`_find_dip_midpoint`
的退化路径"顶部中点 = x 均值 + y 最大"在 V 形翻领上取到的是
V 形右领尖的几何位置（领尖在右上方）。`top_center` 因此落在 583,489
（V 形右领尖），看起来像"右侧口袋附近"。

## 当前可修复方向（任务 #36）

`_find_dip_midpoint` 退化路径要适配"翻领 V 形"——V 形领口的几何
中点应该是 **V 形两领尖的中点**（领尖 y 较浅、V 底 y 较深，取
(y_max + y_v_bottom)/2 处的 x 均值），而不是"V 底 y 对应的 x 均值"。

不同领型需要不同处理（圆领、U 领、高领、POLO 领、V 领），具体
怎么判断领型**待定**——这是任务 #36 的范围。
